;
; A place to test a script before actually adding it
; (perhaps temporarily commit some work in progress)
;

; ~TAB & a::msgbox i'll be damned




; PREFIX

; LControl & F1::return  ; Make left-control a prefix by using it in front of "&" at least once.
; LControl::MsgBox You released LControl without having used it to modify any other key.


; ~a & b::msgbox tf

~WheelRight::ToolTip Right %A_EventInfo%


